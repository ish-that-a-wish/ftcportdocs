---
import Layout from '../layouts/Layout.astro';
---

<Layout title="About: EP Docs">
  <div class="about-hero">
    <div class="hero-gradient"></div>
    <h1 class="fade-in">About This Project</h1>
    <p class="subtitle fade-in" style="--delay: 0.15s">Building better portfolios, one team at a time.</p>
  </div>

  <div class="about-content">
    <!-- Main intro section with image -->
    <div class="intro-section">
      <div class="intro-text">
          <h2 class="slide-in">The Mission</h2>
        <p class="slide-in" style="--delay: 0.1s">
          This website was created in January 2026 to provide a comprehensive, accessible resource for teams looking to create an outstanding Engineering Portfolio for the FIRST Tech Challenge.
        </p>
        <p class="slide-in" style="--delay: 0.2s">
          Whether you're a rookie team building your first portfolio or a veteran team looking to refine your approach, you'll find practical guidance, real examples, and actionable tips to help your team succeed.
        </p>
      </div>
    </div>

    <!-- Tech stack -->
    <div class="tech-section">
      <h2 class="section-title">Built with</h2>
      <div class="tech-stack">
        <div class="tech-item" style="--i: 0">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/></svg>
          <span>Astro</span>
        </div>
        <div class="tech-item" style="--i: 1">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>
          <span>TypeScript</span>
        </div>
        <div class="tech-item" style="--i: 2">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
          <span>CSS</span>
        </div>
        <div class ="tech-item" style="--i: 3">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2z"/><line x1="12" y1="6" x2="12" y2="12"/><line x1="12" y1="12" x2="16" y2="14"/></svg>
            <span>Starlight</span>
        </div>
        <div class="tech-item" style="--i: 4">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10A15.3 15.3 0 0 1 12 2z"/></svg>
          <span>Hosted on Netlify</span>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  /* ── Hero section ─────────────────────────── */
  .about-hero {
    position: relative;
    text-align: center;
    padding: 2.5rem 1rem 2rem;
    margin-bottom: 2rem;
    overflow: hidden;
  }

  .hero-gradient {
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle at 50% 50%, var(--accent-faint) 0%, transparent 50%);
    opacity: 0.6;
    animation: gradient-spin 20s linear infinite;
    pointer-events: none;
  }

  @keyframes gradient-spin {
    0%   { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }

  .about-hero h1 {
    position: relative;
    z-index: 2;
    margin: 0;
    font-size: 2.2rem;
    background: linear-gradient(135deg, var(--accent) 0%, var(--accent-hover) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    position: relative;
    z-index: 2;
    margin-top: 0.5rem;
    font-size: 1rem;
    color: var(--text-muted);
  }

  .fade-in {
    opacity: 0;
    animation: fadeUp 0.8s ease forwards;
    animation-delay: var(--delay, 0s);
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(10px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* ── Main content ─────────────────────────── */
  .about-content {
    max-width: 100%;
  }

  .intro-section {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
    align-items: start;
    margin-bottom: 3rem;
  }

  .intro-text h2 {
    margin-top: 0;
  }

  .intro-text p {
    line-height: 1.75;
  }

  .slide-in {
    opacity: 0;
    animation: slideInLeft 0.7s ease forwards;
    animation-delay: var(--delay, 0s);
  }

  @keyframes slideInLeft {
    from { opacity: 0; transform: translateX(-20px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  /* ── Features grid ────────────────────────── */
  .section-title {
    font-size: 1.4rem;
    margin: 2.5rem 0 1.5rem;
    text-align: center;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.25rem;
    margin-bottom: 3rem;
  }

  .feature-card {
    padding: 1.5rem;
    background: var(--bg-surface-solid);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
    opacity: 0;
    animation: popIn 0.6s ease forwards;
    animation-delay: calc(0.5s + var(--i) * 0.12s);
  }

  @keyframes popIn {
    from { opacity: 0; transform: scale(0.9) translateY(10px); }
    to   { opacity: 1; transform: scale(1) translateY(0); }
  }

  .feature-card:hover {
    transform: translateY(-5px) scale(1.02);
    border-color: var(--border-accent);
    box-shadow: var(--shadow-md);
  }

  .feature-icon {
    color: var(--accent);
    margin-bottom: 0.75rem;
    transition: transform 0.3s ease;
  }

  .feature-card:hover .feature-icon {
    transform: scale(1.1) rotate(5deg);
  }

  .feature-card h4 {
    margin: 0 0 0.5rem;
    font-size: 1.05rem;
    color: var(--text-heading);
  }

  .feature-card p {
    margin: 0;
    font-size: 0.875rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  /* ── Tech section ─────────────────────────── */
  .tech-section {
    padding: 2rem;
    background: var(--bg-surface-solid);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    text-align: center;
  }

  .tech-section h2 {
    margin-top: 0;
  }

  .tech-stack {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    flex-wrap: wrap;
  }

  .tech-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.65rem 1.1rem;
    background: var(--bg-surface-hover);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--text-secondary);
    opacity: 0;
    animation: fadeUp 0.5s ease forwards;
    animation-delay: calc(1.2s + var(--i) * 0.1s);
    transition: transform 0.2s ease, border-color 0.2s ease;
  }

  .tech-item:hover {
    transform: translateY(-2px);
    border-color: var(--border-accent);
  }

  .tech-item svg {
    color: var(--accent);
  }

  /* ── Responsive ───────────────────────────── */
  @media (max-width: 768px) {
    .intro-section {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .about-hero h1 {
      font-size: 1.8rem;
    }

    .features-grid {
      grid-template-columns: 1fr;
    }

    .tech-stack {
      gap: 0.75rem;
    }
  }

  @media (max-width: 480px) {
    .about-hero h1 {
      font-size: 1.5rem;
    }

    .tech-section {
      padding: 1.5rem 1rem;
    }
  }
</style>